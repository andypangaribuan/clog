<clickhouse>
    <listen_host>0.0.0.0</listen_host>
    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <postgresql_port>9005</postgresql_port>
    <interserver_http_port>9009</interserver_http_port>

    <logger>
        <level>information</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <size>1000M</size>
        <count>10</count>
    </logger>

    <disable_internal_dns_cache>0</disable_internal_dns_cache>

    <!--
      Allow ClickHouse to use up to ~90% of available container RAM.
      Query-level limits below provide another layer of protection.
    -->
    <max_server_memory_usage_to_ram_ratio>0.9</max_server_memory_usage_to_ram_ratio>

    <!-- Protect server from pathological connection counts -->
    <max_connections>4096</max_connections>

    <!--
      Prometheus metrics.
      Not published on the host by docker-compose.
      A Prometheus container on the Docker network can scrape:
          http://clickhouse:9363/metrics
    -->
    <prometheus>
        <endpoint>/metrics</endpoint>
        <port>9363</port>
        <metrics>true</metrics>
        <events>true</events>
        <asynchronous_metrics>true</asynchronous_metrics>
        <errors>true</errors>
    </prometheus>

</clickhouse>