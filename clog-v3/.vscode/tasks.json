{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build",
      "type": "shell",
      "command": "cargo build --bin clog && codesign -s - -f --entitlements entitlements.plist target/debug/clog",
      "args": [],
      "group": "build",
      "problemMatcher": ["$rustc"]
    }
  ]
}